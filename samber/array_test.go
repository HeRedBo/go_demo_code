package samber

import (
	"testing"
)

func TestArray(t *testing.T) {
	ArrayDemo()

	// 切片和字符串之间的转化

	//arr := []string{"apple", "banana", "cherry"}
	//str := strings.Join(arr, ", ") // "apple, banana, cherry"
	//arr = strings.Split(str, ",")
	//dump.P(str)
	//dump.P(arr)

}
